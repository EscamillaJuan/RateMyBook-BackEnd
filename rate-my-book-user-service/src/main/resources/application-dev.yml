spring:
  datasource:
    url: ${DB-URI}
    driverClassName: org.h2.Driver
    username: ${DB-USERNAME}
    password: ${DB-PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


token:
  signature:
    secret: ${SECRET}